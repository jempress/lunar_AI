name: lunar_AI
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  lunar_AI:
    git:
      url: https://github.com/jempress/lunar_AI.git
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  velocity_x: ^3.6.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
